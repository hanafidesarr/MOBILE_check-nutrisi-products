import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'check-nutrisi-products',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
